# Week 1 measured results

Place only observed outputs here.

Expected completed layout:

```text
environment/
maf_fides_benign/
camel_benign/
```

Do not manually edit trace values to make a run pass. If instrumentation fails, preserve the failed run under a clearly named directory and explain it in `NOTES.md`.

